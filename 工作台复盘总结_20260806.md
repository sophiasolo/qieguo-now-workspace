# 切果NOW运营工作台 复盘总结文档

**日期**：2026年8月6日  
**复盘范围**：工作台全板块开发 + 部署 + 数据管线

---

## 一、整体架构

```
切果NOW运营工作台
├── 🏠 总览
├── 🛡️ 舆情监控
│   ├── KPI卡片 + 近7天趋势图（Chart.js）
│   ├── 近7天负面舆情列表
│   ├── 舆情明细（今日 + 历史，可筛选正面/负面）
│   └── 数据管线：智慧商情 → 综合看板 → sentiment_summary.json → GitHub Pages
├── ⭐ 精选正面
│   ├── 收藏/取消收藏
│   ├── 导出JSON → download_stars.py 自动下载素材打包zip
│   └── 数据依赖：master_data._imgs（图片直链）
├── 📅 社群排期
│   └── 2026-2027全年，按推送日轮换，支持编辑/重置/导出CSV
├── 📦 产品库
│   └── 拖拽Excel上传 → 解析SKU → 推广推荐+产品总表
├── 📡 热点捕捉
│   ├── 百度/微博/抖音三平台
│   ├── 分类筛选 + 果切高亮
│   └── 每日cron抓取 → hotspot.json → GitHub Pages
├── ✍️ 文案创作（AI）
│   ├── DeepSeek API一键生成社群文案
│   ├── 产品选择器/价格/配送/方向/推送日
│   ├── 套餐说明折叠框
│   ├── 节气品宣模式（输入节日名 → 3版品宣文案）
│   └── 历史记录（20条）
├── 🎨 配图Prompt
│   ├── 产品名+场景 → AI生成Prompt
│   ├── 博主参考固定模板保留
│   ├── 1:1封面 / 3:4社群 用途区分
│   └── ⭐收藏 → 复制 / 豆包生图
├── 🧪 Prompt配方
│   ├── 收藏库按封面/社群分组
│   ├── 多选提炼 → AI分析 → 配方持久化
│   └── ✏️改名 / ▼展开 / 搜索
├── 📚 素材灵感库
│   ├── 🀄 花字卡片（8节日257条 从RTF解析）
│   └── 🍎 水果知识（12种）
└── 💡 AI前沿案例（空壳）
```

---

## 二、已完成功能清单

| 板块 | 功能 | 状态 |
|------|------|:--:|
| 舆情监控 | KPI+趋势+明细+负面列表 | ✅ |
| 舆情监控 | 字段映射（短名t/p/c/u/n → 长名） | ✅ |
| 精选正面 | 收藏+导出JSON+素材下载打包 | ✅ |
| 精选正面 | _imgs图片直链数据管线修复 | ✅ |
| 社群排期 | 2026-2027全年 + 编辑/重置 | ✅ |
| 产品库 | 拖拽Excel上传 + 仪表盘 | ✅ |
| 热点捕捉 | 三平台 + 分类筛选 + 每日cron | ✅ |
| 文案创作 | AI生成 + 套餐说明 + 节气品宣模式 | ✅ |
| 配图Prompt | AI生成 + 博主模板 + 用途区分 | ✅ |
| Prompt配方 | 收藏/多选/提炼/持久化/展开 | ✅ |
| 花字卡片 | RTF解析 8节日257条 | ✅ |
| 水果知识 | 12种水果Tips | ✅ |

---

## 三、重要Bug及根因

### 3.1 字段映射问题（反复出现）

**表现**：舆情明细/负面列表显示[undefined] undefined  
**根因**：`sentiment_summary.json` 用短字段名（t/p/c/u/n），渲染代码用长字段名（title/platform/…），没有映射  
**教训**：**每次引入新数据源，必须检查字段名是否一致，在数据入口统一映射**

### 3.2 代码修改后其他板块丢失

**表现**：修花字库 → 小程序链接消失；恢复v7 → 舆情字段映射丢失  
**根因**：用 v7 恢复文件时丢失了 v8 已打的补丁  
**教训**：
1. **不用 cp v7 → v8 恢复**，用 git checkout 恢复具体commit
2. **每次推送后立即验证所有受影响板块**
3. **打补丁用统一脚本**，不要手工逐行改

### 3.3 Patch工具转义问题

**表现**：`\\n` 变 `\\\\n`，`\\'` 变 `\\\\\\\\'`，导致语法错误  
**根因**：patch 工具的 old_string/new_string 会被多轮转义  
**教训**：**复杂字符串替换用 Python write_file + terminal 执行**，不用 patch

### 3.4 GitHub Pages 缓存

**表现**：推送后用户看不到变化  
**解决**：每次改 JS 必须同步 bump 版本号（v39→v40→…）  
**教训**：**JS/HTML修改 → bump版本号 → commit → push，一步都不能少**

### 3.5 Cron 空数据覆盖

**表现**：热点数据被 cron 生成的空文件覆盖，页面变空白  
**解决**：手动补抓 + cron 脚本加保护（数据<10条不写入）

### 3.6 Chart.js 重复创建

**表现**：切换日期时报 Canvas already in use  
**解决**：创建前 `Chart.getChart(id)?.destroy()`

---

## 四、开发铁律

### 4.1 代码修改
1. ✅ **先用 write_file + Python 脚本改**，不用 patch 做复杂替换
2. ✅ **改完立即 node -c 验证语法**
3. ✅ **每次 push 前 bump 版本号**
4. ✅ **v7 = v8 同步**在 commit 前执行

### 4.2 数据管线
5. ✅ **新数据源 → 字段映射 → 在入口函数统一处理**
6. ✅ **数据为空时显示明确提示，不静默失败**
7. ✅ **cron 脚本加数据量保护**

### 4.3 验证流程
8. ✅ **每改一个板块 → 刷新验证该板块 + 相邻板块**
9. ✅ **Console无报错才算通过**
10. ✅ **历史数据加载/日期切换/筛选都要测**

### 4.4 复盘
11. ✅ **每个bug修完记录根因 + 教训**
12. ✅ **同类错误不要再犯第三次**

---

## 五、当前待处理

| 项目 | 状态 |
|------|:--:|
| 圣诞节花字有碎片需整理 | 🟡 |
| 素材灵感库-灵感图 | ⚪ 待设计 |
| AI前沿案例 | ⚪ 空壳 |
| 社群数据/引流 | 🟡 基础可用 |
| 下载脚本抖音Cookie问题 | 🟡 部分失败 |

---

## 六、文件位置

- 工作台：`~/Desktop/切果NOW工作台/`
- 代码：`index.html` + `js/app_v8.js`
- 数据：`sentiment_summary.json`, `master_data.json`, `hotspot.json`
- 部署：`sophiasolo.github.io/qieguo-now-workspace/`
- 花字源：`~/Desktop/花字库.rtf`
- 下载脚本：`download_stars.py`
- Cron：热点抓取、舆情采集、产品库更新

---

*文档生成时间：2026-08-06*
*下次复查建议：每次大型改动后更新第四条铁律*
