# 切果NOW运营工作台 · 完整复盘总结

**时间跨度**：2026年7月 ~ 2026年8月6日
**版本**：app_v8.js v42 / GitHub Pages
**URL**：https://sophiasolo.github.io/qieguo-now-workspace/

---

## 一、项目起源与演化

### 第一阶段：工具箱时代
- 文案助手 PWA（Gitee → Vercel）
- 配图 Prompt 工具（iframe 嵌入）

### 第二阶段：统一工作台（7月中旬）
- GitHub Pages 部署
- 左侧导航 + 右侧主内容区
- 初始模块：总览、舆情、排期、产品库、热点

### 第三阶段：功能深化（7月下旬~8月初）
- 文案创作：模板 → 纯 AI（DeepSeek API）
- 产品库：表格 → 仪表盘（拖拽 + 推荐）
- 配图 Prompt：iframe → 原生模块
- 新增：Prompt 配方、精选正面、花字库、节气品宣

---

## 二、所有已交付板块

| 板块 | 核心功能 | 状态 |
|------|----------|:--:|
| 舆情监控 | KPI+趋势图+负面列表+明细+筛选+收藏 | ✅ |
| 精选正面 | 收藏/取消+导出JSON+素材打包zip | ✅ |
| 社群排期 | 2026-2027全年+编辑/重置/CSV | ✅ |
| 产品库 | 拖拽Excel解析+仪表盘+标签 | ✅ |
| 热点捕捉 | 三平台+分类+每日cron | ✅ |
| 文案创作 | AI生成+套餐说明+节气品宣3版 | ✅ |
| 配图Prompt | AI生成+博主模板+1:1/3:4用途 | ✅ |
| Prompt配方 | 收藏/多选/提炼/持久化/改名展开 | ✅ |
| 花字卡片 | RTF解析 8节日257条 | ✅ |
| 水果知识 | 12种水果Tips | ✅ |

---

## 三、重要 Bug 及根因

### 🐛 1. 舆情字段映射缺失（反复出现）
| 项 | 内容 |
|----|------|
| **表现** | 明细/负面列表显示 [undefined] undefined |
| **根因** | JSON 用短字段（t/p/c/u/n），渲染用长字段（title/platform/…），没有统一映射 |
| **修法** | loadSentimentData 入口统一映射 + 渲染函数双字段兜底 |
| **教训** | **新数据源第一件事：检查字段名，入口统一转换** |

### 🐛 2. 代码恢复丢失补丁（反复出现）
| 项 | 内容 |
|----|------|
| **表现** | 用 cp v7 → v8 恢复后，之前打的补丁全丢 |
| **根因** | v7 不是最新版，缺少后续补丁 |
| **教训** | **不用 cp 恢复！用 git checkout 恢复具体 commit** |

### 🐛 3. Patch 工具转义漂移
| 项 | 内容 |
|----|------|
| **表现** | \\n → \\\\n → \\\\\\\\n，导致 JS 语法错误 |
| **根因** | patch 工具对 old_string/new_string 多轮转义 |
| **教训** | **复杂替换用 Python write_file + terminal**，不用 patch |

### 🐛 4. GitHub Pages 缓存
| 项 | 内容 |
|----|------|
| **表现** | 推送后用户看不到变化 |
| **解决** | 每次 JS 改动 → bump 版本号（v39→v40→…） |
| **教训** | **JS/HTML 改 → bump 版本号 → 缺一不可** |

### 🐛 5. Cron 空数据覆盖
| 项 | 内容 |
|----|------|
| **表现** | 热点数据被 cron 空文件覆盖 |
| **解决** | 脚本加保护（数据<10条不写入） |

### 🐛 6. Chart.js 重复创建
| 项 | 内容 |
|----|------|
| **表现** | 切换日期报 Canvas already in use |
| **解决** | 创建前 Chart.getChart(id)?.destroy() |

### 🐛 7. 周四自动隐藏小程序链接
| 项 | 内容 |
|----|------|
| **表现** | 小程序链接周四消失 |
| **根因** | initCopyDay 写了自动隐藏逻辑，未经用户同意 |
| **教训** | **任何 UI 行为变更必须先和用户确认** |

### 🐛 8. 历史数据 _imgs 为空
| 项 | 内容 |
|----|------|
| **表现** | 精选正面导出后图片无法自动下载 |
| **根因** | classify_pipeline.py 硬编码 `_imgs = []` |
| **教训** | 检查数据管线每个环节，不丢弃原始数据 |

### 🐛 9. 热点链接缺失
| 项 | 内容 |
|----|------|
| **表现** | 百度/微博条目无原帖链接 |
| **根因** | 手动抓取脚本没带 URL |
| **解决** | 百度用搜索链接兜底，微博用 s.weibo.com 搜索链接 |

---

## 四、12条开发铁律

1. **字段映射先做**：引入新数据源 → 第一件事检查字段名 → 统一转换
2. **不用 cp 恢复**：用 git checkout <commit> 恢复具体版本
3. **不用 patch 做复杂替换**：用 Python write_file + node -c 验证
4. **改 JS/HTML 必 bump 版本号**：push 前一步都不能少
5. **改完立即验证**：本板块 + 相邻板块都要测
6. **Console 零报错**才算通过
7. **Cron 加数据保护**：空数据不覆盖
8. **UI 行为变更先征求用户**：不做擅自决定
9. **每次 push 同步 v7 = v8**
10. **历史数据不丢弃**：管线每个环节检查字段保留
11. **同类错误不犯第三次**：记入复盘 → 下次前置自查
12. **对话结束前输出变更摘要**

---

## 五、待处理清单

| 项目 | 优先级 |
|------|:--:|
| 圣诞节花字碎片整理 | 🟡 |
| 素材灵感库-灵感图设计 | ⚪ |
| AI 前沿案例 | ⚪ |
| 下载脚本抖音 Cookie | 🟡 |
| 日常花字按主题分类 | 🟡 |

---

## 六、关键文件路径

- 工作台代码：`~/Desktop/切果NOW工作台/index.html` + `js/app_v8.js`
- 数据文件：`sentiment_summary.json`, `master_data.json`, `master_data_7d.json`, `hotspot.json`
- 花字源：`~/Desktop/花字库.rtf`
- 部署：GitHub Pages `sophiasolo/qieguo-now-workspace`
- 下载脚本：`download_stars.py`
- 文案创作：`qieguo-now-copy-toolbox` 技能
- 社群排期：`qieguo-now-weekly-content` 技能
- 舆情采集：`qieguo-now-sentiment-daily` 技能

---

*文档生成于 2026-08-06，基于全部对话历史 + Context Summary + Memory*
